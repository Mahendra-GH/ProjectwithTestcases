"""numbers.py
This Module has is_even function it will checks the num is even or not
"""
def is_even(number):
    """this function will checks the number is even or not
     Args:
        number (int): number to be checked
    Returns:
        bool: True if even, False otherwise
    """
    return number % 2 == 0